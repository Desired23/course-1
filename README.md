Cài sẵn Python và Node.js/npm.
Backend: cd course && pip install -r requirements.txt
Nên chạy python manage.py migrate ít nhất 1 lần.
Frontend: cd course_fe && npm install && npm run dev
Nếu dùng start-all.ps1, máy đó phải chạy được ngrok và đã login/add authtoken.