// Previously rendered a hardcoded list of brands (Netflix, Volkswagen, ...) that the
// platform has no real partnership with. There is no "trusted companies" data source,
// so the section is disabled rather than showing fabricated logos.
export function TrustedCompanies() {
  return null
}
